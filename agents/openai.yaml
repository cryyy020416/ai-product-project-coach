interface:
  display_name: "AI 产品项目教练"
  short_description: "用阶段门判断 AI 项目是否成立，再生成方案与简历表达"
  brand_color: "#2563EB"
  default_prompt: "Use $ai-product-project-coach to judge my AI product project with the stage-gate process before generating a plan, eval design, resume bullets, or interview defense."

policy:
  allow_implicit_invocation: true
